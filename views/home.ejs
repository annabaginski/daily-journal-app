<%- include('partials/header') %>



<h1>Home</h1>

<p> <%= content %> </p>

<% posts.forEach( post => { %>
    <h2> <%= post.title %> </h2>
    <p> <%= (post.content).substring(0,100) + '...' %> </p> 
    <a href="/posts/<%= post.title %>"">Read More</a>
    <% }) %>

    <hr>
    
    <a href="/compose"><button class="btn compose"> Compose</button> </a>

<%- include('partials/footer') %>